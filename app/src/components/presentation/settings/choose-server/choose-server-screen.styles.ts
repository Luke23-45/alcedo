import styled from 'styled-components/native';

// Choose-server screen scroll content: the None option, the backend cards,
// and the dashed add-new row.
export const ChooseServerContent = styled.View`
  padding-top: 16px;
  padding-bottom: 28px;
  gap: 12px;
`;
