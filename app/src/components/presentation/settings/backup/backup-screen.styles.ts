import styled from 'styled-components/native';

// Backup screen scroll content: sections separated by the 28pt group rhythm
// (settings-dark.md Screen 6).
export const BackupScreenContent = styled.View`
  padding-top: 16px;
  padding-bottom: 28px;
  gap: 28px;
`;
