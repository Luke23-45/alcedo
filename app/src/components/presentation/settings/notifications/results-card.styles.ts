import styled from 'styled-components/native';

/** Results card (settings-dark.md Screen 3): three 58pt toggle rows. */

export const Block = styled.View`
  padding-top: 8px;
  padding-bottom: 8px;
`;
