import styled from 'styled-components/native';

/** Display card: legacy app-configuration toggles, kept reachable. */

export const Block = styled.View`
  padding-top: 8px;
  padding-bottom: 8px;
`;
