import styled from 'styled-components/native';

/* Sheet background: absolute layers behind the content. */

export const BackgroundLayer = styled.View`
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
`;
