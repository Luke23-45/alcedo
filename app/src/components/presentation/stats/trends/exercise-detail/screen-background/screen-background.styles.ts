import styled from 'styled-components/native';
import type { StyleProp, ViewStyle } from 'react-native';

export const BackgroundLayer = styled.View`
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
`;

export const auraSvg: StyleProp<ViewStyle> = {
  position: 'absolute',
  left: 0,
  top: 0,
};
