import { NotificationsScreen } from '@/components/presentation/settings/notifications/notifications-screen';

/** Notifications (settings-dark.md Screen 3) — thin route wrapper. */
export default function NotificationsPage() {
  return <NotificationsScreen />;
}
